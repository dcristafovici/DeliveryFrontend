import styled from 'styled-components';

export const ErrorMessageStyled = styled.div`
  font-size: 16px;
  font-weight: 500;
  margin-bottom: 30px;
  color: red;
  span {
    font-weight: inherit;
    color: inherit;
    font-size: inherit;
  }
`;
